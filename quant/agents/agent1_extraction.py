"""Agent 1 — SEC EDGAR extraction and cross-reference."""

from __future__ import annotations


def run(ticker: str) -> dict:
    """Pull filing, return structured extraction JSON. See wiki Agents page."""
    raise NotImplementedError("Wire EDGAR ingest + Gemini extraction loop")
