"""Agent 5 — Elastic kNN analog search + MongoDB enrichment."""

from __future__ import annotations


def run(fingerprint: dict, k: int = 10) -> dict:
    """Return top analog days with 30/60/90d outcome distributions."""
    raise NotImplementedError("Wire Elastic MCP kNN + MongoDB context")
