"""Agent 3 — semantic narrative drift vs prior quarter."""

from __future__ import annotations


def run(ticker: str, extraction: dict) -> dict:
    """Return ranked narrative flags with materiality scores."""
    raise NotImplementedError("Wire MongoDB prior quarter + semantic diff")
